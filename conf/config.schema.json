{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "disable": {
      "description": "Disables auth completely (note: this has no effect in production environments)",
      "type": "boolean",
      "default": false
    },
    "tokenSecret": {
      "description": "A secret used to encode/decode Json Web Tokens",
      "type": "string"
    },
    "tokenLifespan": {
      "description": "How long a token should remain valid for",
      "type": "string",
      "default": "99y"
    },
    "tokenIssuer": {
      "description": "The identity of the issuer of the token",
      "type": "string",
      "default": "adapt"
    },
    "sessionSecret": {
      "description": "A secret used to encode/decode user sessions",
      "type": "string"
    },
    "sessionSecret": {
      "description": "How long a token should remain valid for in milliseconds",
      "type": "number",
      "default": 604800000
    }
  },
  "required": ["tokenSecret", "sessionSecret"]
}
