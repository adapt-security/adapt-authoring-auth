{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "disable": {
      "description": "Disables auth (note: this has no effect in production environment)",
      "type": "boolean",
      "default": false
    },
    "secret": {
      "description": "A secret used to encode/decode Json Web Tokens",
      "type": "string"
    },
    "tokenLifespan": {
      "description": "How long a token should remain valid for",
      "type": "string",
      "default": "99y"
    }
  },
  "required": ["secret"]
}
